{% extends 'owner/layout.html.twig' %}

{% block main_body %}
<div class="portlet-body">    
    <div class="table-toolbar">
        <div class="btn-group">
            <a href="{{path('owner_new')}}" class="btn green">
                Add New<i class="icon-plus"></i>
            </a>
        </div>
    </div>
    <table class="table table-striped table-hover table-bordered" id="sample_editable_1">
        <thead>
            <tr>
                <th>ID Number</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Mobile</th>
                <th>Telephone</th>
                <th>Show</th>
                <th>Edit</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('owner_show', { 'id': entity.id }) }}">{{ entity.socialSecurityNo }}</a></td>
                    <td><a href="{{ path('owner_show', { 'id': entity.id }) }}">{{ entity.lastName }}</a></td>
                    <td><a href="{{ path('owner_show', { 'id': entity.id }) }}">{{ entity.firstName }}</a></td>
                    <td>{{ entity.email }}</td>
                    <td>{{ entity.mobile }}</td>
                    <td>{{ entity.telephone }}</td>
                    <td><a href="{{ path('owner_show', { 'id': entity.id }) }}">show</a></td>
                    <td><a href="{{ path('owner_edit', { 'id': entity.id }) }}">edit</a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div> 
{% endblock %}