{% extends 'company/layout.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    <script>
        jQuery(document).ready(function () {
            App.init();
        });
    </script>    
{% endblock %}

{% block main_body %}
    <div class="portlet-body"> 
        <div class="table-toolbar">
            <div class="btn-group">
                {#<a href="{{path('erf_new')}}" class="btn green">
                    Add Erf <i class="icon-plus"></i>
                </a>#}
            </div>
            <div class="btn-group pull-right">
                <button class="btn dropdown-toggle" data-toggle="dropdown">Tools <i class="icon-angle-down"></i>
                </button>
                <ul class="dropdown-menu pull-right">
                    <li><a href="#">Print</a></li>
                    <li><a href="#">Save as PDF</a></li>
                    <li><a href="#">Export to Excel</a></li>
                </ul>
            </div>
        </div>
        <div class="portlet-body form">
            {{ form_start(delete_form) }}
            <div class="form-horizontal form-view">
                <div class="row-fluid">
                    <div class="span6 ">
                        <div class="control-group">
                            <label class="control-label" for="erfNo">Municipality:</label>
                            <div class="controls">
                                <input type="text" value="{{ entity.name }}" readonly="readonly" class="m-wrap span12"/>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row-fluid">
                    <div class="span6 ">
                        <div class="control-group">
                            <label class="control-label" for="address">Physical Address:</label>
                            <div class="controls">
                                <input type="text" value="{{ entity.address }}" readonly="readonly" class="m-wrap span12"/>
                            </div>
                        </div>
                    </div>
                </div>            
                            
                <div class="row-fluid">
                    <div class="span6 ">
                        <div class="control-group">
                            <label class="control-label" for="address">Postal Address:</label>
                            <div class="controls">
                                <input type="text" value="{{ entity.postal }}" readonly="readonly" class="m-wrap span12"/>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row-fluid">
                    <div class="span6 ">
                        <div class="control-group">
                            <label class="control-label" >Email:</label>
                            <div class="controls">
                                <input type="text" value="{{ entity.email }}" readonly="readonly" class="m-wrap span12"/>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row-fluid">  
                    <div class="span6 ">
                        <div class="control-group">
                            <label class="control-label" >Phone:</label>
                            <div class="controls">
                                <input type="text" value="{{ entity.phone }}" readonly="readonly" class="m-wrap span12"/>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row-fluid">
                    <div class="span6 ">
                        <div class="control-group">
                            <label class="control-label" >Website:</label>
                            <div class="controls">
                                <input type="text" value="{{ entity.website }}" readonly="readonly" class="m-wrap span12"/>                            
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="form-actions">
                    <a href="{{ path('company') }}" class="btn">Back to list</a>
                    <a href="{{ path('company_edit', { 'id': entity.id }) }}"  class="btn blue"><i class="icon-pencil"></i> Edit</a>
                    {{ form_widget(delete_form.submit, { 'attr': {'class': 'btn red', 'label': 'Delete'} }) }}
                </div>
            </div>
            {{ form_end(delete_form) }} 
        </div>
    </div>
{% endblock %}